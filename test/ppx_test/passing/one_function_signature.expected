module type S  = sig type 'a t val f : 'a t -> 'a t end[@@deriving
                                                         mica_types]
include
  struct
    type expr =
      | F of expr 
    type ty =
      | IntT 
    let gen_expr ty =
      let open Core.Quickcheck.Generator in
        let open Let_syntax in size >>= (fun x -> return x)
  end[@@ocaml.doc "@inline"][@@merlin.hide ]
